# NEVER commit real credentials here.
# In the GitLab pipeline, kubectl apply replaces this file's values
# with the actual secrets stored in GitLab CI/CD Variables.
apiVersion: v1
kind: Secret
metadata:
  name: backend-db-secret
  namespace: three-tier
type: Opaque
stringData:
  DB_PASSWORD: "REPLACE_AT_DEPLOY_TIME"
